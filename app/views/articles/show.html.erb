<div class="container">
  <h2><%= @article.title %></h2>
  <p><%= @article.content %></p>

  <%= link_to "Edit Article", edit_article_path, :class => 'btn btn-outline-primary' %>
  <%= link_to "Delete", article_path(@article.id), class: "btn btn-default btn-outline-danger", data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
</div>
